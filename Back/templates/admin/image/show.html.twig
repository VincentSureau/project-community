{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <div class="container-fluid container_table_list">
        <h2 class="show-title">Image : </h2>

    <a class="btn btn-light" href="{{ path('image_index') }}">back to list</a>
    <a class="btn btn-success" href="{{ path('image_edit', {'id': image.id}) }}">edit</a>
    {{ include('admin/image/_delete_form.html.twig') }}

    <table class="table table-striped table_list">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>ImageLink</th>
                <td><img class="image_for_list" src="{{ image.imageLink }}" alt=""></td>
            </tr>
            <tr>
                <th>IsHero</th>
                <td>{{ image.isHero ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
