<form class="d-inline" method="post" action="{{ path('professional_status_delete', {'id': professional_status.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional_status.id) }}">
    <button data-toggle="tooltip" data-placement="top" title="Supprimer" class="btn btn-link p-0"><i class="fas fa-trash-alt"></i></button>
</form>
