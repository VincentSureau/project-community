{% extends 'base.html.twig' %}

{% block title %}Afficher une competence{% endblock %}

{% block body %}
    <div class="container-fluid container_table_list">
        <h2 class="show-title">Competence : {{ competence.name }}</h2>

        <a class="btn btn-light" href="{{ path('competence_index') }}">Retour à la liste</a>
        <a class="btn btn-success" href="{{ path('competence_edit', {'id': competence.id}) }}">Modifier</a>
        {{ include('admin/competence/_delete_form.html.twig') }}

        <table class="table table-striped table_list">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ competence.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ competence.name }}</td>
                </tr>
                <tr>
                    <th>Slug</th>
                    <td>{{ competence.slug }}</td>
                </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
