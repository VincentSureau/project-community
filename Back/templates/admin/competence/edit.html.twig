{% extends 'base.html.twig' %}

{% block title %}Modifier la compétence - Admin{% endblock %}

{% block body %}
    <div class="container-form container_table_list">
        <h2 class="show-title">Modifier la compétence</h2>
        <a class="btn btn-light btn-light_edit" href="{{ path('competence_index') }}">Retour à la liste</a>
        {{ include('admin/competence/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div class="d-flex justify-content-center container_delete">
            {{ include('admin/competence/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
