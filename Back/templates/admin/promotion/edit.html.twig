{% extends 'base.html.twig' %}

{% block title %}Modifier la promotion - Admin{% endblock %}

{% block body %}
    <div class="container-form container_table_list">
        <h2 class="show-title">Modifier la promotion</h2>

        {{ include('admin/promotion/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-light" href="{{ path('promotion_index') }}">back to list</a>

        {{ include('admin/promotion/_delete_form.html.twig') }}
    </div>
{% endblock %}
