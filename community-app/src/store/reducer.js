// initial state
const initialState = {};

// reducer
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    // Action non-reconnue
    default:
      return state;
  }
};

// export
export default reducer;
